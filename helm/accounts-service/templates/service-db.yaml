apiVersion: v1
kind: Service
metadata:
  name: {{ include "accounts-service.fullname" . }}-db
  labels:
    {{- include "accounts-service.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
spec:
  ports:
    - port: {{ .Values.db.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "accounts-service.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: database
  clusterIP: None


apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "accounts-service.fullname" . }}-test-connection"
  labels:
    {{- include "accounts-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['-qO-', 'http://{{ include "accounts-service.fullname" . }}:{{ .Values.service.port }}/actuator/health']
  restartPolicy: Never


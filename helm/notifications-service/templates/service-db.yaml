apiVersion: v1
kind: Service
metadata:
  name: {{ include "notifications-service.fullname" . }}-db
  labels:
    {{- include "notifications-service.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
spec:
  ports:
    - port: {{ .Values.db.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "notifications-service.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: database
  clusterIP: None

apiVersion: v1
kind: Service
metadata:
  name: {{ include "blocker-service.fullname" . }}-db
  labels:
    {{- include "blocker-service.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
spec:
  ports:
    - port: {{ .Values.db.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "blocker-service.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: database
  clusterIP: None
